security:
  jwt:
    token:
      secret-key: 53cr37

spring:
  application:
    name: DelAgenda
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:delagenda;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
